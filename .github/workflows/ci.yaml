name: CI for FastAPI

on:
  push:
    branches:
      - main
    
jobs:
  build:
    runs-on: self-hosted

    steps:

    - name: Criar um arquivo no /tmp
      run: |
        echo 'teste de criação de arquivo' > /tmp/thiagoteste.txt
